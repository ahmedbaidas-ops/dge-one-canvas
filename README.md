# DGE One Canvas

Arabic-first prototype of the DGE "One Canvas" with the phone layer and "The airport, linked" concept. Single static file; hash routes (e.g. `#/en/o/answer/a1`). Page-view counts fall back to the visitor's own device on a static host.
